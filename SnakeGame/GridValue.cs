namespace SnakeGame
{
    //A cell, in the grid, may be empty, contain part of the snake or be empty. 
    public enum GridValue
    {
        Empty,
        Snake,
        Food,
        Outside
    }
}
